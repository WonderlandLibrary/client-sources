package club.pulsive.client.intent.yay;


public interface EnvironmentConstants {

	final String BASE = "https://intent.store/api/",
			   API_VERSION = "1.0",
			   USER_AGENT = "Intent-API/" + API_VERSION + " Pulsive";
	
}
